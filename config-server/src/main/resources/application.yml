server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_REPO_URI:https://github.com/meryemciftci/banking-config-repo}
          default-label: main
          clone-on-start: true